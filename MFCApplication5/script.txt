CREATE TABLE IF NOT EXISTS FILE_OBJECT ( FILE_ID int NOT NULL, ITEM_ID int NOT NULL, TYP_ID int NOT NULL, PRIMARY KEY (FILE_ID, ITEM_ID,TYP_ID) ); CREATE TABLE IF NOT EXISTS FILETABLE( FILEPATH TEXT NOT NULL, DATEOFMOD datetime NOT NULL, ID INTEGER PRIMARY KEY AUTOINCREMENT ); CREATE TABLE IF NOT EXISTS FIXTURE( ID INTEGER PRIMARY KEY AUTOINCREMENT, MAKE TEXT NOT NULL, NAME TEXT NOT NULL, FIXTURE_TYPE int NOT NULL, USER_TYPE int NULL, METRIC int NOT NULL, MATCH int NOT NULL, NUM_SIM_FILES int NOT NULL, CHUCH_WIDTH float NOT NULL, GB_DEPTH float NOT NULL, GB_PULLBACK_DIST float NOT NULL, IDT_ID int NOT NULL, FILE TEXT NOT NULL); CREATE TABLE IF NOT EXISTS TOOLBLOCK( ID INTEGER PRIMARY KEY AUTOINCREMENT, IDT_ID int NOT NULL, MAKE TEXT NOT NULL, FILENAME TEXT NOT NULL, METRIC int NOT NULL, OFFSET_X bit NOT NULL, OFFSET_Y bit NOT NULL, OFFSET_Z bit NOT NULL, NUM_ORIENTATIONS int NOT NULL, MATCH int NOT NULL, NUM_SIM_FILES int NOT NULL, TOOL_POS TEXT NOT NULL, CS int NOT NULL, CS_NAME TEXT NOT NULL, MATCH_TOOL_POS int NOT NULL, BLOCK_TYPES TEXT NULL, SHANK_SIZES TEXT NULL, DEFAULT_ORIENT int NOT NULL, DEFAULT_CS TEXT NOT NULL); CREATE TABLE IF NOT EXISTS IDT( ID INTEGER PRIMARY KEY AUTOINCREMENT, DESCRIPTION TEXT NOT NULL);